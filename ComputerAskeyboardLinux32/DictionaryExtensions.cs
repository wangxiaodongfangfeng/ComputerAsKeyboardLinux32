using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ComputerAskeyboardLinux32
{
    public static class DictionaryExtensions
    {
        //public static bool TryGetValue(this Dictionary<EventCode, byte> dic, EventCode key, out byte? value)
        //{
        //    if (!dic.ContainsKey(key)) { value = null; return false; }
        //    value = dic[key];
        //    return true;
        //}
    }
}
